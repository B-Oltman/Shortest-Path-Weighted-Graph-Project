#include <fstream>
#include <cstddef>
#include <iostream>
#include <vector>
#include "sched.hpp"
using namespace std;

int main(int argc, char** argv)
{
    if(argc < 2)
    {
        std::cout << "useage: ./sched.out <stations.dat> <trains.dat>\n";
        return 0;
    }

    int
    bool quit = false;
    while(!quit)
    {
        cin >>
    }
}